version: '3.8'

services:
  backend:
    image: clusterzx/ts6-manager:backend
    container_name: ts6-backend
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3001
      - DATABASE_URL=file:/app/packages/backend/data/ts6webui.db
      - "JWT_SECRET=${JWT_SECRET:?Set JWT_SECRET in .env or environment}"
      - "ENCRYPTION_KEY=${ENCRYPTION_KEY:?set ENCRYPTION_KEY in .env, different from JWT_SECRET - generate with openssl rand -hex 32}"
      - TS_ALLOW_SELF_SIGNED=${TS_ALLOW_SELF_SIGNED:-false}
      - YT_COOKIE_FILE=${YT_COOKIE_FILE:-}
      - JWT_ACCESS_EXPIRY=15m
      - JWT_REFRESH_EXPIRY=7d
      # Must match the origin the browser actually uses — it is the CORS
      # allow-list value. Set FRONTEND_URL in .env for anything but localhost.
      - FRONTEND_URL=${FRONTEND_URL:-http://localhost:3000}
      - MUSIC_DIR=/data/music
      - SIDECAR_URL=http://ts6-sidecar:9800
      - "SIDECAR_TOKEN=${SIDECAR_TOKEN:?set SIDECAR_TOKEN in .env - generate with openssl rand -hex 32}"
      - SIDECAR_BINARY_PATH=${SIDECAR_BINARY_PATH:-}
      - YT_BROWSER_URL=http://ts6-yt-browser:9090
      - YT_BROWSER_NOVNC_URL=http://ts6-yt-browser:6080
      - "YT_BROWSER_TOKEN=${YT_BROWSER_TOKEN:?set YT_BROWSER_TOKEN in .env - generate with openssl rand -hex 32}"
    volumes:
      - backend-data:/app/packages/backend/data
      - music-data:/data/music
      # Optional: mount YouTube cookies file for yt-dlp
      # - ./cookies.txt:/app/cookies.txt
    ports:
      - "3001:3001"
    depends_on:
      - sidecar
      - yt-browser
    networks:
      - ts6-network

  sidecar:
    image: clusterzx/ts6-manager:sidecar
    container_name: ts6-sidecar
    restart: unless-stopped
    environment:
      - SIDECAR_PORT=9800
      # Reachable by the backend over the internal bridge network only —
      # do not publish this port: the API has no per-user authentication.
      - SIDECAR_LISTEN_ADDR=0.0.0.0
      - "SIDECAR_TOKEN=${SIDECAR_TOKEN:?set SIDECAR_TOKEN in .env - generate with openssl rand -hex 32}"
    networks:
      - ts6-network

  yt-browser:
    image: clusterzx/ts6-manager:yt-browser
    container_name: ts6-yt-browser
    restart: unless-stopped
    shm_size: '2gb'
    environment:
      - YT_BROWSER_API_PORT=9090
      - "YT_BROWSER_TOKEN=${YT_BROWSER_TOKEN:?set YT_BROWSER_TOKEN in .env - generate with openssl rand -hex 32}"
    volumes:
      - yt-browser-profile:/data/yt-profile
    networks:
      - ts6-network

  frontend:
    image: clusterzx/ts6-manager:frontend
    container_name: ts6-frontend
    restart: unless-stopped
    ports:
      # Host port is configurable so a deployment never has to patch this
      # tracked file — set FRONTEND_PORT in .env instead.
      - "${FRONTEND_PORT:-3000}:8080"
    depends_on:
      - backend
    networks:
      - ts6-network

volumes:
  backend-data:
    driver: local
  music-data:
    driver: local
  yt-browser-profile:
    driver: local

networks:
  ts6-network:
    driver: bridge
