version: '3.8'

services:
  backend:
    image: clusterzx/ts6-manager:backend-dev
    container_name: ts6-backend-dev
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3001
      - DATABASE_URL=file:/app/packages/backend/data/ts6webui.db
      - "JWT_SECRET=${JWT_SECRET:?Set JWT_SECRET in .env or environment}"
      - "ENCRYPTION_KEY=${ENCRYPTION_KEY:?set ENCRYPTION_KEY in .env, different from JWT_SECRET - generate with openssl rand -hex 32}"
      - JWT_ACCESS_EXPIRY=15m
      - JWT_REFRESH_EXPIRY=7d
      - FRONTEND_URL=${FRONTEND_URL:-http://localhost:3090}
      - MUSIC_DIR=/data/music
      - SIDECAR_URL=http://ts6-sidecar-dev:9800
      - "SIDECAR_TOKEN=${SIDECAR_TOKEN:?set SIDECAR_TOKEN in .env - generate with openssl rand -hex 32}"
      - YT_BROWSER_URL=http://ts6-yt-browser-dev:9090
      - YT_BROWSER_NOVNC_URL=http://ts6-yt-browser-dev:6080
      - "YT_BROWSER_TOKEN=${YT_BROWSER_TOKEN:?set YT_BROWSER_TOKEN in .env - generate with openssl rand -hex 32}"
    volumes:
      - backend-data:/app/packages/backend/data
      - music-data:/data/music
    ports:
      - "3091:3001"
    depends_on:
      - sidecar
      - yt-browser
    networks:
      - ts6-network

  sidecar:
    image: clusterzx/ts6-manager:sidecar-dev
    container_name: ts6-sidecar-dev
    restart: unless-stopped
    environment:
      - SIDECAR_PORT=9800
      - SIDECAR_LISTEN_ADDR=0.0.0.0
      - "SIDECAR_TOKEN=${SIDECAR_TOKEN:?set SIDECAR_TOKEN in .env - generate with openssl rand -hex 32}"
    networks:
      - ts6-network

  yt-browser:
    image: clusterzx/ts6-manager:yt-browser-dev
    container_name: ts6-yt-browser-dev
    restart: unless-stopped
    shm_size: '2gb'
    environment:
      - YT_BROWSER_API_PORT=9090
      - "YT_BROWSER_TOKEN=${YT_BROWSER_TOKEN:?set YT_BROWSER_TOKEN in .env - generate with openssl rand -hex 32}"
    volumes:
      - yt-browser-profile:/data/yt-profile
    networks:
      - ts6-network

  frontend:
    image: clusterzx/ts6-manager:frontend-dev
    container_name: ts6-frontend-dev
    restart: unless-stopped
    ports:
      - "3090:8080"
    depends_on:
      - backend
    networks:
      - ts6-network

volumes:
  backend-data:
    driver: local
  music-data:
    driver: local
  yt-browser-profile:
    driver: local

networks:
  ts6-network:
    driver: bridge
